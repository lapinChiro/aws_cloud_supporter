// JSONOutputFormatter単体テスト
// CLAUDE.md準拠: No any types、TDD実践

// Import split test files
import './json-formatter.basic.test';
import './json-formatter.validation.test';
import './json-formatter.performance.test';

describe('JSONOutputFormatter', () => {
  it('should load all split test files', () => {
    // This test ensures all split files are imported
    expect(true).toBe(true);
  });
});